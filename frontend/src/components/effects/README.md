# Effects provenance

`FadeContent.vue` is a local Vue 3 adaptation of the official Vue Bits FadeContent registry component. The upstream project is distributed under MIT with Commons Clause; this adapter uses the same license terms and keeps the effect decorative. It must not represent business progress or hide task/error state.
